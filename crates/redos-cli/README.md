# cli

## structure

```sh
redos # scans the current directory, alias for redos scan local .
redos scan local [path]* # scans the given paths, defaults to current directory
redos scan git [path] # scans the given git repo
```
